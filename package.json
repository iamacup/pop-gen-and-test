{
  "name": "pop-gen-and-test",
  "version": "1.0.0",
  "description": "A system to generate a popu;ation and automatically test user inputs through the API",
  "main": "./src/app.js",
  "scripts": {
    "start": "nodemon ./src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "tom@tompickard.com",
  "license": "Apache-2.0",
  "dependencies": {
    "axios": "^0.18.0",
    "body-parser": "^1.18.2",
    "express": "^4.16.3",
    "faker": "^4.1.0"
  },
  "devDependencies": {
    "nodemon": "^1.17.4"
  }
}
